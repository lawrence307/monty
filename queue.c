#include "monty.h"

/**
 * queue - opcode that sets the format of the data to a queue (FIFO)
 * @stack: Pointer to the stack.
 * @line_number: Where the queue function is called.
 */

void queue(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
	arguments->stack = 0;
}
