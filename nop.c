#include "monty.h"

/**
 * nop - a function that just does nothing
 * @stack: Pointer to the stack.
 * @line_number: Where the nop function is called.
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void) stack;
	(void) line_number;
}
